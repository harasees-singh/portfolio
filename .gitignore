# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Scratch / agent artefacts. Underscore-prefixed files and `*.diff` /
# `*.patch` blobs are commonly dropped at the repo root by automated
# tooling — ignore them by default so they never sneak into a commit.
_*
*.diff
*.patch
